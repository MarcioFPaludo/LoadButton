    import XCTest
    @testable import LoadButton

    final class LoadButtonTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(LoadButton().text, "Hello, World!")
        }
    }
